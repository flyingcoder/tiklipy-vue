version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"  # Map host port to container port
    env_file:
      - .env
    volumes:
      - ./:/app
